{
  "name": "nself-tv-integration-tests",
  "version": "0.2.0",
  "private": true,
  "description": "Phase 2 integration and contract tests for nself-tv backend services",
  "type": "module",
  "scripts": {
    "test": "vitest run --reporter=verbose",
    "test:watch": "vitest --reporter=verbose"
  },
  "devDependencies": {
    "vitest": "^3.0.0"
  }
}
