# Changelog

All notable repository-structure and planning baseline changes are documented here.

## 2026-02-10

### Changed

1. Moved root docs source from `/docs` to `/.wiki`.
2. Moved `antbox/` to `backend/antbox/`.
3. Moved `antserver/` to `backend/antserver/`.
4. Updated root and component READMEs to match strict layout policy.
5. Added root-layout guard CI workflow and validation script.
6. Added GitHub wiki auto-publish workflow from `/.wiki`.

### Governance

1. Added repository structure policy at `.wiki/00-Repository-Structure-Policy.md`.
2. Enforced temp/planning file placement under `.claude/` or `.codex/`.
